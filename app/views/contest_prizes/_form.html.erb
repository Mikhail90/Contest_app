<%= form_for(@contest_prize) do |f| %>
  <% if @contest_prize.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contest_prize.errors.count, "error") %> prohibited this contest_prize from being saved:</h2>

      <ul>
      <% @contest_prize.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contest_id %><br />
    <%= f.number_field :contest_id %>
  </div>
  <div class="field">
    <%= f.label :prize_id %><br />
    <%= f.number_field :prize_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
