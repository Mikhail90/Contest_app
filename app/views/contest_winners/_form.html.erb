<%= form_for(@contest_winner) do |f| %>
  <% if @contest_winner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contest_winner.errors.count, "error") %> prohibited this contest_winner from being saved:</h2>

      <ul>
      <% @contest_winner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contestant_id %><br />
    <%= f.number_field :contestant_id %>
  </div>
  <div class="field">
    <%= f.label :prize_id %><br />
    <%= f.number_field :prize_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
